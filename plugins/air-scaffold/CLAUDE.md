# air-scaffold

Project scaffolding plugin for Claude Code.

## Release

1. Update version in: `.claude-plugin/plugin.json`
2. Update version in: `../../.claude-plugin/marketplace.json` (air-scaffold entry)
3. Create git tag: `git tag air-scaffold-v<version>`
